function openEnvelope() {
    const wrapper = document.getElementById('envelopeWrapper');
    // Class 'open' ကို ထည့်လိုက်ရင် CSS က Animation တွေ စလုပ်ပါလိမ့်မယ်
    wrapper.classList.add('open');
}
